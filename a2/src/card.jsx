export function Card(props){
    //feature plans
        //props.title - 
        //props.subtitle - (optional) 
        //props.text - main body text
        //props.show - feature flags
            //show.title
            //show.subtitle
            //show.image
            //show.text
            //show.actions
        //props.image.url - (optional) the url of an image
        //props.image.alt - (optional) the alt text for an image, requires props.url
        //props.actions - 
    // end docs
    if (props.show.title){

    }
    if (props.show.image){

    }
    //show body text

    //show actions
    if (props.show.actions){

    }

}