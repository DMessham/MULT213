//define a user object
export class User {
    constructor(name) {
        this.name = name,
        this.move = move
    }
}

//greeter
export function greetUser(user){
    return "Hello, " + user.name + "!";
};
